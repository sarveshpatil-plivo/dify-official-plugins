[project]
name = "dify-plivo-voice"
version = "0.1.0"
description = "Plivo Voice tool plugin for Dify"
readme = "README.md"
requires-python = ">=3.12"

# Managed with uv; refresh the lockfile with `uv lock`.
dependencies = [
    "dify-plugin>=0.9.0",
    "plivo>=4.60.1",
]

[dependency-groups]
dev = []
